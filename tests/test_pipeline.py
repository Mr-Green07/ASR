"""Start here: latency-budget assertions per stage, router fast-path table
tests, response-manager sentence splitting (abbreviations!), tool schema
generation, and an end-to-end text-only agent turn against a mocked LLM.
"""
